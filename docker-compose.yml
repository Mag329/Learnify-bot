version: '3.8'
services:
  app:
    container_name: mesh_bot
    build: ./
    env_file: .env
    environment:
      - TZ=Europe/Moscow
      - TMPDIR='/var/tmp'
    volumes:
      - .:/src
    restart: always